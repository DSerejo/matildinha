{
  "name": "matildinha",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd ng && yarn install --production=false",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cd ng && yarn install && yarn build --prod",
    "heroku-postbuild": "tsc && cd ng && yarn build --prod && rm -rf src && cd .. && cp -r ng/dist/ng dist/ng &&  rm -rf ng",
    "start": "node dist/server.js",
    "start:dev": "tsnd --respawn --transpile-only src/server.ts"
  },
  "cacheDirectories": [
    "node_modules",
    "ng/node_modules"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "typescript": "^4.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "@types/node": "^15.6.1",
    "ts-node-dev": "^1.1.6"
  },
  "engines": {
    "node": "^14.0.0"
  }
}
